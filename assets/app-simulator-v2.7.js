(() => {
  const $ = s => document.querySelector(s), $$ = s => [...document.querySelectorAll(s)];
  const scenarioId = new URLSearchParams(location.search).get("id") || "pc-hardware";
  const categoryLabels={motherboard:"Motherboards",cpu:"Processors",paste:"Thermal Compound",cooler:"CPU Coolers",ram:"Memory",storage:"Storage",gpu:"Graphics",psu:"Power Supplies",casefan:"Case Cooling"};
  const categoryIcons={motherboard:"MB",cpu:"CPU",paste:"TIM",cooler:"FAN",ram:"RAM",storage:"SSD",gpu:"GPU",psu:"PSU",casefan:"FAN"};
  const slotTypes=["motherboard","cpu","paste","cooler","ram","storage","gpu","psu","casefan"];
  let scenario=null,catalog=[],attemptCounter=0;
  const storageKey=`saa-simulator-${scenarioId}`;
  const state={active:false,selectedId:null,focusedInstalled:null,installed:Object.fromEntries(slotTypes.map(k=>[k,null])),ramSlots:{A1:null,A2:null,B1:null,B2:null},cables:{atx:false,eps:false,gpu:false,cpuFan:false},powered:false,post:null,validated:false,validationSnapshot:null,diagnostics:0,changes:0};

  const todayValue=()=>{const d=new Date(),off=d.getTimezoneOffset();return new Date(d.getTime()-off*60000).toISOString().slice(0,10)};
  const money=n=>`$${Number(n||0).toFixed(0)}`;
  const esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[c]));
  const comp=id=>catalog.find(x=>x.id===id)||null;
  const installed=type=>comp(state.installed[type]);
  const setConsole=(message,code="INFO")=>{$("#consoleMessage").textContent=message;$("#consoleCode").textContent=code};
  const installedRams=()=>Object.entries(state.ramSlots).map(([slot,id])=>({slot,component:comp(id)})).filter(x=>x.component);
  const totalMemoryGB=()=>installedRams().reduce((sum,x)=>sum+(x.component.capacityGB||0),0);
  const memoryMode=()=>{const used=installedRams().map(x=>x.slot),hasA=used.some(x=>x.startsWith("A")),hasB=used.some(x=>x.startsWith("B"));return used.length>=2&&hasA&&hasB?"Dual Channel":used.length?"Single Channel":"None"};
  const totalCost=()=>slotTypes.filter(t=>t!=="ram").reduce((sum,t)=>sum+(installed(t)?.price||0),0)+installedRams().reduce((sum,x)=>sum+(x.component.price||0),0);
  const installedCount=()=>slotTypes.filter(t=>t!=="ram"&&state.installed[t]).length+installedRams().length;

  function resetRunState(){state.selectedId=null;state.focusedInstalled=null;slotTypes.forEach(t=>state.installed[t]=null);Object.keys(state.ramSlots).forEach(k=>state.ramSlots[k]=null);state.cables={atx:false,eps:false,gpu:false,cpuFan:false};state.powered=false;state.post=null;state.validated=false;state.validationSnapshot=null;state.diagnostics=0;state.changes=0}

  function compatibilityIssues(){
    const issues=[],mb=installed("motherboard"),cpu=installed("cpu"),cooler=installed("cooler"),rams=installedRams(),gpu=installed("gpu"),psu=installed("psu");
    if(mb&&cpu&&mb.socket!==cpu.socket)issues.push(`CPU socket ${cpu.socket} does not match motherboard socket ${mb.socket}`);
    if(mb)rams.forEach(({slot,component:ram})=>{if(mb.memoryType!==ram.memoryType)issues.push(`${ram.memoryType} memory in DIMM ${slot} is incompatible with a ${mb.memoryType} motherboard`)});
    if(cpu&&cooler&&!(cooler.sockets||[]).includes(cpu.socket))issues.push(`CPU cooler does not support ${cpu.socket}`);
    if(gpu?.requiresPower&&psu&&!psu.hasGpuPower)issues.push("Power supply does not provide the required GPU power connector");
    return issues;
  }

  const requiredWattage=()=>250+(installed("gpu")?.wattage||0);

  function postBlockingIssues(){
    const issues=[],mb=installed("motherboard"),cpu=installed("cpu"),rams=installedRams(),psu=installed("psu"),gpu=installed("gpu"),cooler=installed("cooler");
    if(!mb)issues.push("motherboard missing");if(!cpu)issues.push("CPU missing");if(!rams.length)issues.push("memory missing");if(!psu)issues.push("power supply missing");
    issues.push(...compatibilityIssues());
    if(mb&&!state.cables.atx)issues.push("24-pin ATX power disconnected");if(cpu&&!state.cables.eps)issues.push("8-pin EPS CPU power disconnected");
    if(cpu&&!state.installed.paste)issues.push("thermal compound missing");if(cpu&&!cooler)issues.push("CPU cooler missing");if(cooler&&!state.cables.cpuFan)issues.push("CPU_FAN disconnected");
    if(psu&&psu.wattage<requiredWattage())issues.push(`power supply capacity is below the estimated ${requiredWattage()} W requirement`);
    const videoAvailable=(cpu?.integratedGraphics&&mb?.displayOutputs>0)||!!gpu;if(!videoAvailable)issues.push("no graphics output available");
    if(gpu?.requiresPower&&!state.cables.gpu)issues.push("GPU power disconnected");
    return [...new Set(issues)];
  }

  function requirementResults(){
    if(!scenario)return[];
    const r=scenario.requirements||{},mb=installed("motherboard"),cpu=installed("cpu"),rams=installedRams(),storage=installed("storage"),gpu=installed("gpu"),psu=installed("psu"),cooler=installed("cooler");
    const ramGB=totalMemoryGB(),mode=memoryMode(),compat=compatibilityIssues(),videoOutputs=gpu?.displayOutputs||((cpu?.integratedGraphics&&mb)?mb.displayOutputs:0),results=[];
    if(r.memoryGB!=null)results.push({key:"memory",label:`Memory capacity: at least ${r.memoryGB} GB`,detail:rams.length?`${ramGB} GB installed • ${mode} • slots ${rams.map(x=>x.slot).join(", ")}`:"No memory installed",pass:ramGB>=r.memoryGB});
    if(r.dualChannelRequired)results.push({key:"dual-channel",label:"Memory configuration: Dual Channel required",detail:rams.length?`${mode} • populated slots ${rams.map(x=>x.slot).join(", ")}${mode==="Dual Channel"?"":" • use matched modules across channels; A2/B2 are preferred for a two-DIMM build"}`:"No memory installed",pass:mode==="Dual Channel"});
    if(r.storageGB!=null){const cap=r.storageGB>=1000?`${r.storageGB/1000} TB`:`${r.storageGB} GB`,iface=r.storageInterface?` ${r.storageInterface}`:"";results.push({key:"storage",label:`Storage: at least ${cap}${iface} ${String(r.storageType||"").toUpperCase()}`.replace(/\s+/g," ").trim(),detail:storage?`${storage.name} • ${storage.capacityGB} GB • ${storage.interface||"interface not specified"}`:"No storage installed",pass:!!storage&&storage.capacityGB>=r.storageGB&&(!r.storageType||storage.storageType===r.storageType)&&(!r.storageInterface||String(storage.interface||"").toLowerCase()===String(r.storageInterface).toLowerCase())})}
    if(r.minCpuCores!=null||r.minCpuThreads!=null||r.minCpuTier!=null){const checks=[];if(r.minCpuCores!=null)checks.push(`${r.minCpuCores}+ cores`);if(r.minCpuThreads!=null)checks.push(`${r.minCpuThreads}+ threads`);if(r.minCpuTier!=null)checks.push(`performance tier ${r.minCpuTier}+`);results.push({key:"cpu-workload",label:`CPU workload requirement: ${checks.join(" • ")}`,detail:cpu?`${cpu.name} • ${cpu.cores??"?"} cores / ${cpu.threads??"?"} threads • performance tier ${cpu.performanceTier??"?"}`:"No CPU installed",pass:!!cpu&&(r.minCpuCores==null||(cpu.cores||0)>=r.minCpuCores)&&(r.minCpuThreads==null||(cpu.threads||0)>=r.minCpuThreads)&&(r.minCpuTier==null||(cpu.performanceTier||0)>=r.minCpuTier)})}
    if(r.dedicatedGpuRequired)results.push({key:"dedicated-gpu",label:"Graphics: dedicated GPU required",detail:gpu?`${gpu.name} installed`:cpu?.integratedGraphics?`${cpu.integratedGraphicsName||"Integrated graphics"} provides display output, but it does not satisfy the dedicated-GPU requirement`:"No dedicated GPU installed",pass:!!gpu});
    if(r.gpuVramGB!=null)results.push({key:"gpu-vram",label:`Graphics memory: at least ${r.gpuVramGB} GB VRAM`,detail:gpu?`${gpu.name} • ${gpu.vramGB||0} GB VRAM`:"No dedicated GPU installed",pass:!!gpu&&(gpu.vramGB||0)>=r.gpuVramGB});
    if(r.minGpuTier!=null)results.push({key:"gpu-performance",label:`GPU performance: tier ${r.minGpuTier}+`,detail:gpu?`${gpu.name} • performance tier ${gpu.performanceTier||0}`:"No dedicated GPU installed",pass:!!gpu&&(gpu.performanceTier||0)>=r.minGpuTier});
    if(r.minCoolingTier!=null)results.push({key:"cooling",label:`CPU cooling: tier ${r.minCoolingTier}+`,detail:cooler?`${cooler.name} • cooling tier ${cooler.coolingTier||0}`:"No CPU cooler installed",pass:!!cooler&&(cooler.coolingTier||0)>=r.minCoolingTier});
    if(r.minPsuWattage!=null)results.push({key:"psu",label:`Power supply: at least ${r.minPsuWattage} W`,detail:psu?`${psu.name} • ${psu.wattage||0} W`:"No power supply installed",pass:!!psu&&(psu.wattage||0)>=r.minPsuWattage});
    if(r.displayOutputs!=null)results.push({key:"display",label:`Display support: ${r.displayOutputs} output${r.displayOutputs===1?"":"s"}`,detail:gpu?`${videoOutputs||0} usable output(s) through dedicated graphics`:cpu?.integratedGraphics?`${videoOutputs||0} usable output(s) through ${cpu.integratedGraphicsName||"integrated graphics"}`:"No usable graphics output",pass:(videoOutputs||0)>=r.displayOutputs});
    if(r.budget!=null)results.push({key:"budget",label:`Budget: ${money(r.budget)} maximum`,detail:`Current component cost: ${money(totalCost())}`,pass:totalCost()>0&&totalCost()<=r.budget});
    results.push({key:"compat",label:"Hardware compatibility",detail:compat.length?compat[0]:"Installed platform components are physically compatible",pass:compat.length===0&&!!mb&&!!cpu&&rams.length>0&&!!cooler});
    const assemblySafe=!!mb&&!!cpu&&!!state.installed.paste&&!!cooler&&rams.length>0&&!!storage&&!!psu&&!!installed("casefan")&&state.cables.atx&&state.cables.eps&&state.cables.cpuFan&&(!gpu?.requiresPower||state.cables.gpu)&&psu.wattage>=requiredWattage();
    results.push({key:"assembly",label:"Complete and safe assembly",detail:assemblySafe?`Required hardware, cooling and power connections complete • estimated load ${requiredWattage()} W`:"Build or required connections are incomplete",pass:assemblySafe});
    results.push({key:"post",label:"Successful POST",detail:state.post===true?"POST completed successfully":state.post===false?"POST failed":"POST not yet tested",pass:state.post===true});
    return results;
  }

  const buildComplete=()=>{const r=requirementResults();return state.validated&&r.length>0&&r.every(x=>x.pass)};
  function canInstall(type){if(!state.active)return"Start an attempt before installing components.";if(type!=="motherboard"&&!installed("motherboard"))return"Install a motherboard before adding components to the board or case.";if(type==="paste"&&!installed("cpu"))return"Thermal compound is applied only after the CPU is seated.";if(type==="cooler"&&!installed("cpu"))return"Install the CPU before installing its cooler.";if(type==="cooler"&&!installed("paste"))return"Apply thermal compound before mounting the CPU cooler.";return null}

  function populateCategories(){const categories=[...new Set(catalog.map(c=>c.type))];$("#componentCategory").innerHTML=categories.map(t=>`<option value="${esc(t)}">${esc(categoryLabels[t]||t)}</option>`).join("");renderInventory()}
  function renderInventory(){const type=$("#componentCategory").value||slotTypes[0],items=catalog.filter(c=>c.type===type);$("#inventoryCount").textContent=`${items.length} option${items.length===1?"":"s"}`;$("#inventoryList").innerHTML=items.map(c=>{const isInstalled=c.type==="ram"?installedRams().some(x=>x.component.id===c.id):state.installed[c.type]===c.id,selected=state.selectedId===c.id;return `<button class="simPart ${selected?"isSelected":""} ${isInstalled?"isInstalled":""}" data-component="${esc(c.id)}" type="button" ${!state.active?"disabled":""} aria-pressed="${selected?"true":"false"}"><span class="simPartIcon" aria-hidden="true">${esc(categoryIcons[c.type]||"PC")}</span><span><strong>${esc(c.name)}</strong><small>${esc(c.detail)}</small></span><span class="simPartPrice"><span class="srOnly">Scenario price </span>${money(c.price)}</span></button>`}).join("");$$(".simPart").forEach(btn=>btn.addEventListener("click",()=>{state.selectedId=btn.dataset.component;state.focusedInstalled=null;const c=comp(state.selectedId);$("#selectedPart").textContent=c.name;$("#selectedPartHelp").textContent=`${c.detail} • Scenario price ${money(c.price)}. Choose a compatible installation target.`;const research=$("#researchPartLink");if(c.researchUrl){research.href=c.researchUrl;research.hidden=false;research.setAttribute("aria-label",`Open manufacturer specifications for ${c.name} in a new tab`)}else{research.hidden=true;research.removeAttribute("href")}setConsole(`${c.name} selected. Choose the correct workbench location.`,"SELECT");renderInstalledActions();renderInventory()}))}

  function renderInstalledActions(){const box=$("#installedActions"),remove=$("#removeInstalledBtn");if(!box||!state.focusedInstalled){if(box)box.hidden=true;return}const ref=state.focusedInstalled,c=ref.ramSlot?comp(state.ramSlots[ref.ramSlot]):installed(ref.type);if(!c){box.hidden=true;state.focusedInstalled=null;return}box.hidden=false;$("#installedActionName").textContent=ref.ramSlot?`${c.name} — DIMM ${ref.ramSlot}`:c.name;$("#installedActionHelp").textContent=ref.type==="motherboard"?"Removing the motherboard also removes dependent CPU, cooling, memory, storage, and graphics components.":"Removal is explicit so an accidental click cannot undo your work.";remove.setAttribute("aria-label",`Remove ${c.name}${ref.ramSlot?` from DIMM ${ref.ramSlot}`:""}`)}
  function focusInstalled(ref){state.selectedId=null;state.focusedInstalled=ref;const c=ref.ramSlot?comp(state.ramSlots[ref.ramSlot]):installed(ref.type);if(c)setConsole(`${c.name} selected for inspection. Use the explicit Remove component button if you intend to uninstall it.`,"INSPECT");renderInstalledActions();renderInventory()}
  function removeFocusedInstalled(){const ref=state.focusedInstalled;if(!ref)return;const c=ref.ramSlot?comp(state.ramSlots[ref.ramSlot]):installed(ref.type);if(!c)return;if(ref.ramSlot)state.ramSlots[ref.ramSlot]=null;else{const target=ref.type;if(target==="motherboard"&&!window.confirm("Remove the motherboard and all components installed on it?"))return;state.installed[target]=null;if(target==="motherboard"){["cpu","paste","cooler","ram","storage","gpu"].forEach(t=>state.installed[t]=null);Object.keys(state.ramSlots).forEach(k=>state.ramSlots[k]=null);state.cables={atx:false,eps:false,gpu:false,cpuFan:false}}if(target==="cpu"){state.installed.paste=null;state.installed.cooler=null;state.cables.eps=false;state.cables.cpuFan=false}if(target==="cooler")state.cables.cpuFan=false;if(target==="gpu")state.cables.gpu=false;if(target==="psu")state.cables={...state.cables,atx:false,eps:false,gpu:false}}state.focusedInstalled=null;state.powered=false;state.post=null;state.validated=false;state.changes++;setConsole(`${c.name} removed from the build.`,"REMOVE");render()}

  function updateSlot(type){const c=installed(type),el=$("#state-"+type);if(!el)return;if(type==="paste")el.textContent=c?"Applied":"Not applied";else if(type==="gpu")el.textContent=c?c.name:"Optional";else el.textContent=c?c.name:"Empty";const slot=$(`[data-slot="${type}"]`);if(slot){slot.classList.toggle("isInstalled",!!c);slot.classList.remove("isIncompatible");slot.setAttribute("aria-label",c?`${categoryLabels[type]} installation area. ${c.name} installed. Press Enter to inspect.`:`${categoryLabels[type]} installation area. Empty.`)}}
  function renderRamSlots(){Object.keys(state.ramSlots).forEach(slot=>{const c=comp(state.ramSlots[slot]),el=document.querySelector(`[data-ram-slot="${slot}"]`),label=document.getElementById(`state-ram-${slot}`);if(el){el.classList.toggle("isInstalled",!!c);el.setAttribute("aria-label",c?`DIMM ${slot}. ${c.name} installed. Press Enter to inspect.`:`DIMM ${slot}. Empty${["A2","B2"].includes(slot)?". Preferred population slot":""}.`)}if(label)label.textContent=c?`${c.capacityGB} GB ${c.memoryType}`:"Empty"})}
  function render(){slotTypes.forEach(updateSlot);const results=requirementResults(),passed=results.filter(x=>x.pass).length,pct=results.length?Math.round(passed/results.length*100):0;$("#attemptNumber").textContent=attemptCounter;$("#installedStatus").textContent=installedCount();$("#costStatus").textContent=money(totalCost());$("#powerStatus").textContent=state.powered?"ON":"OFF";$("#postStatus").textContent=state.post===true?"PASS":state.post===false?"FAIL":"—";$("#requirementsStatus").textContent=`${passed}/${results.length}`;$("#progressPercent").textContent=`${pct}%`;$("#progressBar").style.width=`${pct}%`;$("#checklist").innerHTML=results.map(x=>`<div class="simCheck ${x.pass?"done":state.validated?"fail":""}"><span class="simCheckIcon" aria-hidden="true">${x.pass?"✓":state.validated?"!":""}</span><span><strong>${esc(x.label)}</strong><small>${esc(x.detail)}</small></span></div>`).join("");const active=state.active;["resetBtn","powerBtn","diagnoseBtn","inspectBtn","validateBtn"].forEach(id=>$("#"+id).disabled=!active);const gpu=installed("gpu");$("#cableAtx").disabled=!active||!installed("psu")||!installed("motherboard");$("#cableEps").disabled=!active||!installed("psu")||!installed("cpu");$("#cableCpuFan").disabled=!active||!installed("cooler");$("#cableGpu").disabled=!active||!installed("psu")||!gpu||!gpu.requiresPower;$("#cableAtx").checked=state.cables.atx;$("#cableEps").checked=state.cables.eps;$("#cableCpuFan").checked=state.cables.cpuFan;$("#cableGpu").checked=state.cables.gpu;$("#completionBox").hidden=!buildComplete();$("#startAttemptBtn").textContent=attemptCounter?"Start New Attempt":"Start Attempt";const mb=installed("motherboard"),chipset=$("#board-chipset");if(chipset)chipset.textContent=mb?.chipset||"CHIPSET";renderRamSlots();renderInstalledActions();renderInventory()}

  function installSelected(target){if(!state.active){setConsole("Start an attempt before using the workbench.","WAIT");return}if(installed(target)){focusInstalled({type:target});return}if(!state.selectedId){setConsole(`Choose a ${categoryLabels[target].toLowerCase()} option from the component catalog first.`,"WAIT");return}const c=comp(state.selectedId);if(c.type!==target){setConsole(`${c.name} cannot be installed in the ${categoryLabels[target].toLowerCase()} area.`,"INVALID");return}const block=canInstall(target);if(block){setConsole(block,"ORDER");return}state.installed[target]=c.id;state.selectedId=null;state.focusedInstalled=null;state.powered=false;state.post=null;state.validated=false;state.changes++;setConsole(`${c.name} installed. Installed count ${installedCount()}; current scenario cost ${money(totalCost())}.`,"INSTALL");render()}
  function installRam(slot){if(!state.active){setConsole("Start an attempt before using the workbench.","WAIT");return}if(comp(state.ramSlots[slot])){focusInstalled({type:"ram",ramSlot:slot});return}if(!state.selectedId){setConsole(`Choose a memory module from the component catalog, then select DIMM ${slot}.`,"WAIT");return}const c=comp(state.selectedId);if(c.type!=="ram"){setConsole(`${c.name} cannot be installed in DIMM ${slot}. Select a memory module.`,"INVALID");return}if(!installed("motherboard")){setConsole("Install a motherboard before adding memory.","ORDER");return}state.ramSlots[slot]=c.id;state.selectedId=null;state.focusedInstalled=null;state.powered=false;state.post=null;state.validated=false;state.changes++;setConsole(`${c.name} installed in DIMM ${slot}. Total memory: ${totalMemoryGB()} GB • ${memoryMode()} • scenario cost ${money(totalCost())}.`,"INSTALL");render()}

  function saveRecord(){try{localStorage.setItem(storageKey,JSON.stringify({attemptCounter,technician:$("#technicianName").value.trim(),date:$("#activityDate").value}))}catch(e){}}
  function loadRecord(){try{const raw=localStorage.getItem(storageKey);if(!raw)return;const saved=JSON.parse(raw);attemptCounter=Number(saved.attemptCounter)||0;if(saved.technician)$("#technicianName").value=saved.technician;if(saved.date)$("#activityDate").value=saved.date}catch(e){}}
  function startAttempt(){const name=$("#technicianName").value.trim();if(!name){setConsole("Enter the technician name before starting an attempt.","IDENTITY");$("#technicianName").focus();return}if(!$("#activityDate").value)$("#activityDate").value=todayValue();attemptCounter++;resetRunState();state.active=true;saveRecord();setConsole(`Attempt ${attemptCounter} started for ${name}. Review the scenario requirements and select your first component.`,"ATTEMPT");render()}
  function validate(){state.validated=true;const results=requirementResults(),failed=results.filter(x=>!x.pass);state.validationSnapshot=results;if(!failed.length)setConsole("VALIDATION PASSED. The build meets all scenario requirements and is ready for the completion report.","COMPLETE");else setConsole(`Validation found ${failed.length} unmet requirement${failed.length===1?"":"s"}: ${failed.slice(0,2).map(x=>x.label).join("; ")}${failed.length>2?`; plus ${failed.length-2} more`:""}.`,"VALIDATE");render()}

  function generateReport(){if(!buildComplete())return;const name=$("#technicianName").value.trim(),date=$("#activityDate").value,results=requirementResults(),components=[...slotTypes.filter(t=>t!=="ram").map(t=>installed(t)).filter(Boolean),...installedRams().map(x=>({...x.component,name:`${x.component.name} (DIMM ${x.slot})`}))],id=`SAA-SIM-${Date.now()}`;const rows=components.map(c=>`<tr><td>${esc(categoryLabels[c.type])}</td><td>${esc(c.name)}</td><td>${esc(c.detail)}</td><td>${money(c.price)}</td></tr>`).join("");const reqRows=results.map(r=>`<tr><td>${esc(r.label)}</td><td>${esc(r.detail)}</td><td class="pass">PASS</td></tr>`).join("");const html=`<!doctype html><html><head><meta charset="utf-8"><title>${esc(scenario.title)} — Completion Report</title><style>body{font-family:Arial,sans-serif;color:#171923;margin:36px;line-height:1.45}h1{font-size:24px;margin:0 0 4px}h2{font-size:16px;margin-top:26px;border-bottom:1px solid #ccd;padding-bottom:6px}.sub{color:#555;margin-bottom:22px}.meta{display:grid;grid-template-columns:1fr 1fr;gap:8px 24px;padding:14px;background:#f5f5fa;border:1px solid #ddd;border-radius:8px}.meta p{margin:0}table{width:100%;border-collapse:collapse;margin-top:10px;font-size:12px}th,td{border:1px solid #d7d7df;padding:7px;text-align:left;vertical-align:top}th{background:#f2f2f7}.pass{font-weight:bold}.story{padding:12px 14px;border-left:4px solid #7651c9;background:#f8f7fc}.footer{margin-top:28px;font-size:11px;color:#666}@media print{body{margin:18mm}}</style></head><body><h1>PC Hardware Simulation Completion Report</h1><div class="sub">Server Admin Academy • Support Operations</div><div class="meta"><p><b>Technician:</b> ${esc(name)}</p><p><b>Date:</b> ${esc(date)}</p><p><b>Scenario:</b> ${esc(scenario.title)}</p><p><b>Attempt:</b> ${attemptCounter}</p><p><b>Result:</b> Successful</p><p><b>POST:</b> PASS</p><p><b>Total Cost:</b> ${money(totalCost())}</p><p><b>Report ID:</b> ${esc(id)}</p></div><h2>Scenario</h2><div class="story">${esc(scenario.story)}</div><h2>Final Component Selection</h2><table><thead><tr><th>Category</th><th>Component</th><th>Specification</th><th>Cost</th></tr></thead><tbody>${rows}</tbody></table><h2>Requirement Validation</h2><table><thead><tr><th>Requirement</th><th>Final Evidence</th><th>Status</th></tr></thead><tbody>${reqRows}</tbody></table><h2>Activity Metrics</h2><table><tbody><tr><th>Attempts Used</th><td>${attemptCounter}</td><th>Diagnostic Runs</th><td>${state.diagnostics}</td></tr><tr><th>Build Changes</th><td>${state.changes}</td><th>Completion Status</th><td>Validated</td></tr></tbody></table><div class="footer">This report reflects the final validated state recorded by the Server Admin Academy PC Hardware Simulator. Use the browser print dialog and choose Save as PDF for submission.</div><script>window.onload=()=>setTimeout(()=>window.print(),200)<\/script></body></html>`;const w=window.open("","_blank");if(!w){setConsole("The report window was blocked. Allow pop-ups for this site and try again.","REPORT");return}w.document.open();w.document.write(html);w.document.close()}

  async function init(){$("#activityDate").value=todayValue();loadRecord();try{const res=await fetch("./data/simulations.json",{cache:"no-store"});if(!res.ok)throw new Error("simulation data unavailable");const all=await res.json();scenario=all.find(s=>s.id===scenarioId)||all[0];catalog=scenario.components||[]}catch(err){setConsole("Simulation data could not be loaded. Refresh the page or verify data/simulations.json is deployed.","ERROR");console.error(err);return}$("#scenarioTitle").textContent=scenario.title;$("#scenarioSummary").textContent=scenario.summary;$("#scenarioStory").textContent=scenario.story;$("#requirementChips").innerHTML=(scenario.requirementLabels||[]).map(x=>`<span class="simRequirementChip">${esc(x)}</span>`).join("");document.title=`${scenario.shortTitle||scenario.title} • Server Admin Academy`;populateCategories();render()}

  $("#componentCategory").addEventListener("change",()=>{state.selectedId=null;state.focusedInstalled=null;$("#selectedPart").textContent="None";$("#selectedPartHelp").textContent="Choose a component, then choose its installation area.";$("#researchPartLink").hidden=true;renderInstalledActions();renderInventory()});
  $$("[data-slot]").forEach(slot=>slot.addEventListener("click",e=>{e.stopPropagation();installSelected(slot.dataset.slot)}));
  $$("[data-ram-slot]").forEach(slot=>slot.addEventListener("click",e=>{e.stopPropagation();installRam(slot.dataset.ramSlot)}));
  [["cableAtx","atx","24-pin ATX"],["cableEps","eps","8-pin EPS"],["cableGpu","gpu","PCIe GPU power"],["cableCpuFan","cpuFan","CPU_FAN"]].forEach(([id,key,label])=>$("#"+id).addEventListener("change",e=>{state.cables[key]=e.target.checked;state.powered=false;state.post=null;state.validated=false;state.changes++;setConsole(`${label} ${e.target.checked?"connected":"disconnected"}.`,"CABLE");render()}));
  $("#technicianName").addEventListener("change",saveRecord);$("#activityDate").addEventListener("change",saveRecord);$("#startAttemptBtn").addEventListener("click",startAttempt);$("#resetBtn").addEventListener("click",()=>{resetRunState();state.active=true;setConsole(`Attempt ${attemptCounter} build reset. This remains the same attempt.`,"RESET");render()});$("#removeInstalledBtn")?.addEventListener("click",removeFocusedInstalled);
  $("#powerBtn").addEventListener("click",()=>{state.powered=true;const blockers=postBlockingIssues();state.post=blockers.length===0;state.validated=false;if(state.post)setConsole("POST SUCCESSFUL. Firmware hardware checks passed. Now validate whether the hardware also meets the scenario workload requirements.","POST PASS");else setConsole("POST FAILED. The system has a hardware, compatibility, cooling, graphics, or power issue. Run diagnostics to investigate.","POST FAIL");render()});
  $("#diagnoseBtn").addEventListener("click",()=>{state.diagnostics++;const blockers=postBlockingIssues();if(blockers.length)setConsole(`Diagnostics: ${blockers.slice(0,3).join("; ")}${blockers.length>3?`; plus ${blockers.length-3} additional issue(s)`:""}.`,"DIAG");else setConsole("Diagnostics complete: no POST-blocking hardware faults detected. Workload requirements are evaluated separately.","HEALTHY")});
  $("#inspectBtn").addEventListener("click",()=>{const names=[...slotTypes.filter(t=>t!=="ram").map(t=>installed(t)?.name).filter(Boolean),...installedRams().map(x=>`${x.component.name} in ${x.slot}`)];setConsole(names.length?`Installed hardware: ${names.join(", ")}. Memory mode: ${memoryMode()}. Current cost: ${money(totalCost())}.`:"The workbench is empty. Choose a motherboard platform to begin.","INSPECT")});
  $("#validateBtn").addEventListener("click",validate);$("#reportBtn").addEventListener("click",generateReport);init();
})();